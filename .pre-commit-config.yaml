repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: debug-statements
      - id: mixed-line-ending
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.2
    hooks:
      - id: ruff-format
        exclude: tests
      - id: ruff-check
        exclude: doc
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v2.0.0
    hooks:
      - id: mypy
        # Keep the existing test exclusion for type checking.
        exclude: "tests/"
        additional_dependencies: [
            numpy,
            # Type stubs
            types-paramiko,
            types-PyYAML,
            types-requests,
            types-toml
          ]
