name: new object store test 

on:
  pull_request:
    branches:
      - '*'

jobs:
  your_job_name:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

