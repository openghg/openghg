- Test file that writes to local object store
- Schema for storage of data in the object store?
- How to apply the UUIDs for each block of data / sensor reading within the data we have so far?

- Look at the data that's in the xarray.Datasets, how is that arranged? 
- Mock up schema




